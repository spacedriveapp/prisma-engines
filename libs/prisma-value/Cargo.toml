[package]
edition = "2021"
name = "prisma-value"
version = "0.1.0"

[dependencies]
base64 = "0.22"
chrono = { version = "0.4", features = ["serde"] }
once_cell = "1.20"
regex = "1"
bigdecimal = "0.4"
serde.workspace = true
serde_json.workspace = true
uuid.workspace = true
